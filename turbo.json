{
  "globalDotEnv": [".env.development"],
  "pipeline": {
    "build": {
      "outputs": ["dist/**"],
      "dependsOn": ["^build"]
    },
    "lint": {
      "inputs": ["src/**"]
    },
    "format": {
      "inputs": ["src/**"]
    },
    "test-all": {},
    "clean": {},
    "lint-staged": {}
  }
}
